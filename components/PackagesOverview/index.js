import React, { Component } from 'react';
import { View, Text, FlatList } from "react-native";

class PackagesOverview extends Component {
    render() {
        return(
            <Text>Package overview</Text>
        )
    }
}

export default PackagesOverview;
