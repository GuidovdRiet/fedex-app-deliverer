import React, { Component } from 'react';
import { Text } from "react-native";

class Scan extends Component {
    render() {
        return(
            <Text>Scan a package</Text>
        )
    }
}

export default Scan;